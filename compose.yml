services:
  backend:
    image: backend
    container_name: backend
    build:
      context: /backend
      dockerfile: dockerfile
    ports:
      - 8000:8000
  frontend:
    image: frontend
    container_name: frontend
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "5173:5173"
