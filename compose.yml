services:
  backend:
    build:
      context: /backend
      dockerfile: dockerfile
    ports:
      - 8000:8000
  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "5173:5173"
