# Main backend template
trigger:
  branches:
    include: 
      - dev
  paths:
    include:
      - backend

pool:
  name: "self-agent-pool"

stages:
  - stage: build
  displayName: Build
  jobs:
    - template: build-job-template.yml
